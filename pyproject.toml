[project]
name = "sms-spam-classification"
version = "0.1.0"
description = "SMS Spam Classification application"
readme = "README.md"
authors = [
    { name = "Rigved334", email = "brighved221@gmail.com" }
]
package-mode = false
requires-python = ">=3.14"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "streamlit",
    "joblib",
]

[dependency-groups]
dev = [
    "notebook",
    "ipykernel",
]

[tool.uv]
package = false
